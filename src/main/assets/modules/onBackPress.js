window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-1') {
  
        closeActivity(1);
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-2') {
  
        closeActivity(2);
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-3') {
  
        closeActivity(3);
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-4') {
  
        closeActivity(4);
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-5') {
  
        closeActivity(5);
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-6') {
  
        closeActivity(6);
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-7') {
  
      closeScreenSaverSettings()
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-8') {
  
        closeActivity(8);
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-9') {
  
        closeActivity(9);
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-10') {
  
      closeemergencyAlerts();
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-11') {
  
      closeActivity(11);
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-12') {
  
      closeActivity(12);
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-13') {
  
      closeLocationServices();
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-14') {
  
      closeActivity(14);
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-15') {
  
      closeActivity(15);
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-16') {
  
      closeActivity(16);
    } else{
  
    }
  });



  
  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-17') {
  
      closeActivity(17);
    } else{
  
    }
  });


    
  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-18') {
  
      closeLanguages();
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-19') {
  
      closeKeyboardSettings();
    } else{
  
    }
  });


  
  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-20') {
  
      closeLiveTranslate();
    } else{
  
    }
  });

    
  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-21') {
  
      closeAddLanguage();
    } else{
  
    }
  });


      
  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-22') {
  
      closeDateAndTime();
    } else{
  
    }
  });



  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-23') {
  
      closeBackupSettings();
    } else{
  
    }
  });


  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-24') {
  
      closeRules();
    } else{
  
    }
  });


  
  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-25') {
  
      closeAppInfo();
    } else{
  
    }
  });


  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-26') {
  
      closeSettingServices();
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-27') {
  
      closeSettingServicesAppPermissions();
    } else{
  
    }
  });



  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-28') {
  
      closeSettingsServicesStorageCache();
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-29') {
  
      closeSettingsServicesMobiledata();
    } else{
  
    }
  });


  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-30') {
  
      closeSettingsServicesScreenTime();
    } else{
  
    }
  });



  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-31') {
  
      closeSettingsServicesBatteryUsage();
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-32') {
  
      closeSettingsServicesOpenByDefault();
    } else{
  
    }
  });



  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-33') {
  
      closeWellbeingDashBoard();
    } else{
  
    }
  });

  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-34') {
  
      closeWellbeingFocusMode();
    } else{
  
    }
  });



  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-35') {
  
      closeDisplaySizeAndText();
    } else{
  
    }
  });


  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-36') {
  
      closeSetLock();
    } else{
  
    }
  });




  window.addEventListener('popstate', function (event) {
    const state = event.state;
    if (state && state.modal === 'modal-37') {
      
      closeActivity(37)
    } else{
  
    }
  });